"""
XML = data + meta-data
      text
      hiyerarşik
 i) Well-formed Document     -> Parse
ii) Valid  (xsd, XML Schema) -> Process
SAX Parser
    * read-only
    * event-driven
DOM Parser
    * read/write
"""